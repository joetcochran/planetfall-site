# Tools

Almost every problem in this project ended in a tool: a script, a test, a page or a written rule that stops the problem coming back. This page catalogues them, grouped by the work they serve. Each entry gives the path, what it is for, when it was born, with the commit, and why. Paths are relative to the `Planetfall/` project folder; files served by the site (`scene/`, `dashboard/`, `data/` and the `.html` pages) are under `wwwroot/`. Every script carries a header comment that says more. A few tools lived only in a session's scratch folder or in Claude Code itself; they are marked as not in the repository.

## Hosting and data

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `Program.cs` (the ASP.NET Core host) | Serves the site as static files, re-checked on every load. It also serves `/design/rooms.json`, `/design/packages.json`, `/design/file`, `/design/mail.json` and `POST /design/accept`, the one route that writes, and maps `.md` for this wiki. It plays no part in gameplay | 9 Sep (baseline bd88341); design routes 8d141b0, 5c9a0eb, 8f18eac; mail.json c4022cc; no-cache 35b59a4 | The user wanted the viewer to run from Visual Studio with F5, with no separate server and no CDN |
| `scripts/vendor.mjs` | Copies the Three.js runtime files into `wwwroot/vendor` and stamps them with the current time | 9 Sep, rewritten from the starter | Windows kept source timestamps and defeated the incremental build |
| `scripts/extract_world.py` | A structural ZIL reader, not an interpreter. It writes `world.json` (rooms, exits, descriptions as condition trees, objects, doors, scenery words) and `review.json` | Starter; schema 2 on 9 Sep morning; schema 3 in 1d345c8 | The source has no known way to be compiled (its own README says so), so the data had to be read out of it |
| `scripts/validate.py` | Checks the shape of `world.json` and `layout.json` | Starter; extended 9 Sep | A guard on the extractor |
| `.github/workflows/release.yml` (at the repository root) | The release: on every push to main, on Linux, it runs every test suite and the layout and scene checks, builds the host, builds the site, and publishes it to a separate public repository that GitHub Pages serves. Nothing is published unless every check passes, and since 14 September it also runs `check-site.mjs` on the built site. It publishes only a site that changed: it compares the build's `site-hash.txt` with the published one and skips the push, which is the whole site, when they match. The public repository held one commit, "Publish the site", replaced on each release, until 23 September, when it began keeping its history so that a push sends only changed files. Since 22 September the job first frees about 25 GB of unused toolchains, because the art and the git history had filled the build machine's disk and every run since the last good Pages build (15 September, by the 23 September playtest log) had failed. Since 23 September the checkout is blobless and sparse, and fetches only the pictures the site hangs, listed by `build-site.mjs --list FILE`; the clean-up runs in the background alongside the tests | 14 Sep (c4fb332); the v5 actions, on Node 24, 293bd02; the site-hash skip 8f671d5; the room check fd8c0b5; the disk 88db0df9; sparse checkout and kept history c4df9c2e, adca7f6e; background clean-up 563ba63a | The user: "use github pages for the hosting, for now". Pages is not available on the private source repository, so the site is published from a second, public one. Most pushes are mail and records, which build the same site, and one push of the site's roughly 284 MB had taken 16 minutes |
| `scripts/release.mjs` | The release on a rhythm: a standing order from the user, 2026-09-17, to release morning and evening, once each, if there have been commits. Written to refuse rather than to publish something wrong, because a push is public and cannot be taken back: it will not push unless the branch is main, the remote is reachable, there is at least one commit the remote lacks, `build-site.mjs --dry --tracked` passes and every test file passes. Uncommitted work is reported but does not block -- it simply is not in the release. "Nothing to release" exits 0, nothing here commits, and nothing here pushes any branch but main. It pushes `--follow-tags`, so a tag such as `design-handover-point` travels with the release instead of living on one machine. Since the afternoon of 17 September every test file has a five-minute limit: a hung test holds the release exactly as a failing one does and says which it was, and a test over 15 seconds is named while it runs. Since 18 September a test file that times out is run once more and the release says so: a stall that repeats still holds the release, a one-off does not. Commits are now gated on `release.mjs --dry` rather than on `npm test`, whose single chain stalls intermittently after check-seed. Until 22 September it announced "Released" on the push and never looked at the workflow, so a run of failures read as successes; it now waits for the run, and since 23 September also for the GitHub Pages build of that commit, before it says the site is published. A run cancelled by a newer push is reported as superseded, not failed | 17 Sep (b26baea); `--follow-tags` bf7993c; the per-test limit bd53f0d; the one rerun 0f79f1c; waits for the workflow 88db0df9, and for Pages 563ba63a | The release builds from a CLEAN CHECKOUT, so a painting that was never committed ships its room unpainted. The schedule itself is session-only, so the script is the durable half. `tests/check-seed.mjs` stalled a release check for its full five minutes three times on the night of 17 to 18 September and passed in under 2 seconds on the next run each time. Its spawned runs got a 60-second limit (f24756c). The cause is still open. Norm's note for the night puts the stalls down to agents rendering while the tests ran, and says the gate passes cleanly when nothing else is running |
| `scripts/build-site.mjs` | Builds the public site as static files in `dist/`: the game, the world map and this wiki. It writes the host's design routes out as plain files, copying only what the game hangs: every view of an accepted room, chosen by the same rule the game uses (`hungViews`), its state variants, and the decal pictures. It leaves out the dashboard, the review page and the user's review notes (`room-status.json`), and writes `data/painted-rooms.json`, the list of accepted rooms, and `site-hash.txt`. It fails if an accepted room's registered painting is missing. `--dry` lists what it would write; `--tracked` counts only committed files, for a check before a push; `--preview ROOM` builds a scratch site as if the room were accepted; `--list FILE` writes the pictures the site hangs, for the release's sparse fetch. It writes to a file because on Linux a piped list was cut at 64 KB | 14 Sep (c4fb332); the missing-painting failure, `--tracked` and the hash 8f671d5; `hungViews` fd8c0b5; `--preview` 397b6e5; `--list` c4df9c2e, to a file adca7f6e | A static host has no routes, and drafts, the mailbox and the ledger stay private. The build first shipped only views named `TURN-*`, so on the site eight accepted rooms had black holes where their north and south views should be |
| `scripts/check-site.mjs` | Reads a built site (or the live one, with `--url`) the way the hosted game reads it, and fails if any accepted room is not whole: a view the game cannot find, level views that do not close a ring, or a missing cap. The release runs it before publishing. Since 22 September a room that has one view by design, such as the escape pod's seat in the web, declares `metadata.singleView` with a required reason, and the ring test skips it | 14 Sep (fd8c0b5); `singleView` 6534ef8f | The user, playing the hosted game: "in sanfac A, playing the game, why is there such blackness?" Local play and the review page read files through the host, so they never show a view the static build left out |

## The engine and the game

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `wwwroot/engine/core.js` | The game state machine, with helpers named after the ZIL, the original turn order, a seeded random generator, save and load, and a notes list of unported routines | 9 Sep, milestone 1 | The heart of the hand port |
| `wwwroot/engine/parser.js` | The parser and noun resolver, plus the menu builders shared by the page and the tests. It holds `reachable()`, the click contract | 9 Sep; `reachable()` in b0aa81d | Typing and clicking must reach the same commands |
| `wwwroot/engine/verbs.js` | Default verb behaviour ported from `verbs.zil` | 9 Sep | The defaults every object falls back on |
| `wwwroot/engine/describe.js`, `conditions.js` | Render a room's description tree by evaluating the stored ZIL conditions | 9 Sep | A third of the rooms compute their descriptions |
| `wwwroot/rules/` (nine area modules and `index.js`) | Hand-ported routines, one module per area; `index.js` merges them in a fixed order. A module may export `heldInVehicle`, which says when a vehicle will not let the player go, so that the click menu's "Take all" leaves what is outside the vehicle alone only while that holds | 9–10 Sep (bd88341, 9fd94c1, 1d345c8, 2ed44e0, 2ab5898); `heldInVehicle` b487d77 | The port itself, split so parallel agents could each own a module. With standing implicit, "Take all" from the safety web would have skipped the towel and the survival kit in the one turn they matter, with the pod sinking |
| `wwwroot/ui/ui.js`, `wwwroot/game.js` | The page: transcript, command line, menus, compass, inventory. A `window.planetfall` test hook, with `?seed=` and `?fresh` flags. Since 14 September the game hangs the painted views of every room the user has accepted, chosen as the review page chooses them; the list comes from `data/painted-rooms.json` on the public site, or from `room-status.json` on the dev server. A dark room is drawn dark: no painting, no fabric, no sky and nothing to click, while the text still works; in the dark Transportation Supply the engine adds canon's "There is light to the south." Since 16 September `game.js` resolves a VIEW ROOM on every render: a room may list an art room in `altViews` with the condition that selects it, and while the condition holds the game takes that room's scene, ring, lighting and eye. Only a painted alternative is taken. Since 17 September the compass is a rose with fixed slots, so a spot on screen means the same direction in every room and an empty slot cannot be clicked, and `rules.roomNote` lets a room put what the player must see at a glance on the status line (Underwater's "depth 3 of 3"). `game.js` records the room the scene was built for, as `planetfall.drawnRoom()` | 9 Sep; hook in b0aa81d; paintings f31720b; dark rooms 8c25e4f; art rooms 238dea6; compass rose and `roomNote` bf9f499; `drawnRoom` c935077 | Both ways of playing, and repeatable browser tests. Before the paintings reached the game, a player saw only grey rooms. The Transportation Supply, unlit in canon, came out daylit. Climbing into the pod's safety web does not change room, so the seat inside the webbing -- built, painted, checked and registered, with the whole descent in its porthole -- was never drawn |

## The world map

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `wwwroot/map.html`, `map.js` | The original starter's Three.js map viewer, now at `/map.html` | Starter; moved 9 Sep | The first thing that existed |
| `wwwroot/layout.js`, `data/layout.json` | Places rooms on a grid per region from their exits, with hand anchors per region | 9 Sep (bd88341) | The user wanted compass directions to read as screen directions |
| `scripts/check_layout.mjs` | Reports region extents, overlaps and exits that do not fit; fails on shared cells | 9 Sep | Holds the layout. It still reports 105 of 105 placed |

## Testing the game

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `scripts/play.mjs` | Headless run of the opening with checks, or of any commands | 9 Sep | The first smoke test |
| `scripts/lib/harness.mjs` | Composes any set of rule modules and plays steps through the parser | 9 Sep; `walk()` in b0aa81d | Every suite and the walkthrough stand on it |
| `scripts/tests/*.mjs` (area suites) | One suite per rule module, plus the suites for the tools. Together they make `npm test`. `pod.mjs`, from 16 September, pins the three deviations the user decided for the escape pod under DR-112: the viewport never polarizes, the web holds the player from the explosion to the thud, and standing is implicit | 9 Sep onward; `pod.mjs` b487d77 | 945 checks by 10 Sep. Each of the three deviations is easy to undo by accident, and each contradicts the source |
| `scripts/coverage.mjs` | Lists, per region, the source routines that still lack a handler | 9 Sep | Measured the port. Its blind spot for verb routines was closed by the syntax audit |
| `scripts/tests/walkthrough.mjs` | The canonical seeded playthrough. Every step is typed and must also be clickable | 9 Sep (b0aa81d); to the end 10 Sep (0eabc12) | The user had to type "stand up" in the escape pod |
| `scripts/browser.mjs` | Replays the walkthrough in headless Chromium with real clicks. Playwright is kept outside the repository. Since 16 September it waits for the page's test hook rather than racing it, skips the one 404 the game makes on purpose (a file built for the public site only), and, when a compass button is missing, reports the clock, whether the room is lit and what the compass does offer. Since 17 September it runs on the shared review server, refuses the user's port 5012 even when asked for it, and gives the server back on any exit; and it waits until the game reports the room it has drawn is the room the player is in before reading a pickable or the compass, which took it from 218 failures in 245 steps to 298 steps with none. Since 18 September `--stop-after "<cmd>"` ends the run after a named step, to repeat the early stretch. It must run alone: three walkthroughs at once all failed at "get in web", a step that takes 7 s on its own | 9 Sep (b0aa81d); full game a70e212; the gate's own three faults 12499c6, b11ee36; off the user's port 1b69439; the drawn-room wait c935077; `--stop-after` ddc51a1 | Proves the real page, not just the engine. A gate that cannot be got green on a correct page stops being read: one race had turned into 270 failures, and the diagnosis named the room the player had just left |
| `scripts/tests/menus.mjs` | The click-menu contract and the parser checks from every playtest round, including the `syntax.zil` audit in section 9 | 10 Sep (7decc74); audit ab12e19 | So each round's fixes stay fixed |
| `scripts/check_scenes.mjs` | Validates the per-room scene files against `world.json`. A scene may declare `viewOf`, the game room a second eye belongs to, and its rules, exits, switches and validation resolve through it; since 16 September a room may also carry an explicit `eye`, and both halves of an art-room link are checked -- the art room's `viewOf` and the game room's `altViews` entry with its condition | 10 Sep (48acae1); `viewOf` 1c39c8a; `eye` ac9bfdc; both halves 238dea6 | Scene files had no check. A room gets one eye and one ring, and the player spends the pod's whole descent inside the webbing, which is not the middle of the box. A link with only its first half is an art room the game never reaches |
| `scripts/tests/artroom.mjs` | Pins the art-room routing: every link in both directions, an art room no room routes to as the failure it tests for, the pod's own condition against a real game, that `game.js` still consults `altViews`, and that each of the sixteen beats of the descent resolves to exactly one state | 16 Sep (238dea6) | The fault it guards against broke nothing and failed no check: the art was right and the game simply never drew it |

## Playtesting

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `scripts/playtest.mjs` | A black-box front end for blind testers: one command per call, typed or click-only, with checkpoint starts at the tower core and Lawanda. Since round thirteen its status line carries the room's note through `harness.mjs statusLine` (the climb reads "depth 2 of 3"), and a click session's directions have fixed numbers, 21 north to 32 out (`meta.fixedExits`, so older sessions keep theirs). Since round sixteen it honours RESTART as the page does, at the death prompt and when typed twice while alive; a checkpoint session still starts again with `--new` | 10 Sep (a252eab); checkpoints 471086a, e8979fe; status line and fixed directions 05a700f; RESTART not yet committed | "an agent that knows nothing about the game". Round thirteen's tester saw "Underwater" four times running and could not tell it was climbing. Round sixteen's harness answered RESTART only with "start again with --new", which the tester's rules counted as a second start |
| `scripts/compare_playtests.mjs`, `scripts/lib/friction.mjs` | A table across runs: milestones reached, deaths and friction counts. Since 18 September it counts friction rows through `lib/friction.mjs`, which reads a diary's friction log as a table, whatever shape it has, and is shared with `work-categories.mjs`; it keeps only the room name from the status line | 10 Sep (711f90c); `lib/friction.mjs` 469d1f8; the room name 05a700f | To judge rounds by numbers, not impressions. The first count matched only rows that began with a pipe and a number, so it reported round twelve's 44 and 42 rows, written without a leading pipe, as none, and it missed click rows numbered by turn |
| `playtests/` | 29 first-person diaries with friction logs, and a README of rounds and lessons | 10 Sep; date folders 21c705a; rounds 13 to 16 on 18 Sep; the user's own playtest log, `playtests/2026-09-23/PLAYTEST-LOG.md`, 23 Sep: 25 items, each with its diagnosis, triaged only at the end | The user wanted "a narrative about the decision making" |
| Checkpoints: `scripts/lib/checkpoints.mjs`, `scripts/checkpoints.mjs`, `data/checkpoints/`, `engine/checkpoint.js`, `tests/checkpoints.mjs` | Start the game partway through, at `?checkpoint=ID` or from the debug panel. Each checkpoint is a route of ordinary commands played from the start at seed 1, plus the facts that must hold where it ends, so it is a position the game can really reach; `checkpoints.mjs` writes the saved games and the test fails when a committed one no longer matches its route. Six on 23 September: comm-room, comm-fixed, bio-lock, mini-card, microbe, aux-booth. On load the player is set fed, rested and well (`CHECKPOINT-REFRESH` in `rules/survival.js`), which is a testing aid and not part of the game; see [Where we left the original](canon-deviations.md) | 23 Sep (b5a5e3a9); the refresh 73a416d8, bbed4544; the others 8da0faea, 3c2667e3, 7b06d7c8, 777488a4 | The user: "test the enunciator and chemical dumping mechanism without going through the whole opening", during their own playthrough |
| A/B/C triage and the "Deliberate deviation" marker | A: build it. B: needs the user's approval, and is marked in code. C: leave as the original | 10 Sep (bf7d1c5) | The user allowed "a little bit of common sense deviation" |

## The design process

| Tool | What it is for | Born | Why |
|---|---|---|---|
| Room design packages (`RoomPolishInstructions/<ROOM>_Visual-Design-Handoff/`) | Greg's spec for each room: README, comps and metadata | 10 Sep (eef7c52) | The look needed an owner |
| `CONVENTIONS.md`, `INDEX.md`, regional guides | Coordinates, hotspot tables, the dropped-item fan, palettes | 10 Sep (7decc74) | Norm's six review asks |
| `DESIGN-REQUESTS.md` and `DESIGN-REQUESTS-ARCHIVE.md` | The numbered request ledger with the working order; closed requests archived verbatim | 11 Sep (0a863ef, 3710e1f); archive 0145a93 | One place for every finding, then a dialogue |
| `DESIGN-REVIEW-CONTRACT.md` | Roles, statuses, who may write what, the delivery checklist, the mailbox rules. Section 6b lets Norm correct metadata; since 14 September Norm writes the rest of the Kalamontee metadata under it. Item 14 of the delivery checklist, added by Greg on 14 September, has Greg run `review-room.mjs` before delivering any view of a joined room | 11 Sep (3710e1f); version 6 on 13 Sep; item 14 e9e86bc | "a contract that you both have access to" |
| Ingestion contract | The user's override-layer model, replaced by direct package metadata once Greg had write access | 11 Sep (283577d, bb2ccb8) | The first way designs entered the build |
| `ROOM-METADATA-SCHEMA.json`, `ROOM-PACKAGE-INDEX.json`, `SHARED-ROOM-METADATA.json` | The vocabulary of room metadata, the room-to-package map, and shared defaults. A package may carry `supersededImages`: views that were painted and approved for a room that has since changed, kept with their files so the record survives and no tool builds from them. Since 20 September it also carries `states.<STATE>.noVisibleChange`, the written reason a switch changes nothing on screen, which the toggle gate reads -- a field `shots.mjs` had documented and read since it was written, but which had never been in the schema | 11 Sep (bb2ccb8); lighting be88f13; cameras b9f3334; decals 72c427f; `contentsAt`, added by Greg, bd9a029; `supersededImages` 9ea7e0f | Machine-readable designs. "Scrap what we've got" had to mean something other than deleting twenty-five approved paintings |
| `scripts/check_design.mjs` | Greg's pre-delivery checker: schema, ids, doors, lane and Floyd clearance, day phases, shared doorways. Since 20 September it warns when one `states` key answers both the painted variants and the lighting grade, which is how the shuttle cabins' MOVING was narrowed for the pictures and took the light with it. Since 16 September it also refuses a hung view whose id carries an `@` but which has no `state` field, naming the condition to set, and any registration whose bearing, pitch or field of view disagrees with the sidecar delivered beside the picture. It also understands an ART ROOM: a package may name a room `world.json` does not have, provided its scene says whose view it is (`viewOf`). Since 17 September it refuses two base pictures at one camera, and a hung picture whose condition is not in the game's `when` language ("lacks OPENBIT", "is true"), which would evaluate to false and never show; a `states` condition that does not parse is a warning, because some older packages describe events there. Also since 17 September: a lift or other routine exit is compared with the landing that names it back, a diagonal `wall` must stand in its own quadrant, and a room's exit table and its own openings are compared, as a warning because an exit and an opening are not always the same hole. Four rooms inside the computer (Station 384 and the strip) are on a no-Floyd list, because Floyd dies before the player can get there. Since 18 September it matches states by condition as the review page's presets do, and warns on a base view whose state does not parse. An opening can carry a `clear` size for a slanted or lintelled hole, and UP and DOWN exits skip the position check. Together these took the design warnings from 55 to 11 with no geometry changed | 11 Sep (992386c); states, `clear` and the UP/DOWN skip ddc51a1; lifts, diagonals and exits against openings af52f62; no Floyd in the computer 245f016; the Feinstein rooms excused a Floyd spot 7536a38; a thing the rules bring into a room (the pod's survival kit and towel) accepted 929d9c2; the `state` rule 34e7e29; the camera rule eca602b; art rooms 9ea7e0f; one camera, one base, and parseable conditions a24701b | Most review findings were mechanical. Five variants were painted, approved and registered without a `state` field and could never be drawn. Two views were hung at lenses they were not painted at, and the camera rule then caught a third, Norm's own. On 17 September it passed a bulk registration that doubled every Admin Corridor cap, and seven packages carried conditions that read as English and were always false |
| `ARTWORK-BRIEF.md` | The rulebook for views, states, separate assets and evidence. Version 1.1 added the underpaint method, 1.2 looking at joins, 1.3 seeds from fabric | 12 Sep (264a87e) | Greg's list of what a brief should cover |
| `ARTWORK-AUDIT.md` | All 51 packages and 128 images judged against the brief | 12 Sep (264a87e) | Keep usable art, replace what fails |

## Dashboard and review

| Tool | What it is for | Born | Why |
|---|---|---|---|
| Planetfall Dashboard (`dashboard.html`, `dashboard/`) | Where every room stands, who holds it, the working order, the ledger feed and a walkable preview; opens at `/`. Since 20 September an art room -- a second eye on a room the game has, such as the four underwater depths or the seat in the pod's safety web -- has a review link of its own on the row of the room it is a view of, sorted by eye height so a climb reads as a ladder. They are links and not rooms: every count on the page still counts the game's 105 | 11 Sep (5c9a0eb); six stages 5d2d931 | "track some progress with a dashboard" |
| `data/room-status.json` | The per-room stages, the current holder and the user's verdicts. Since 17 September earlier stages left blank are backfilled from evidence -- each dated by the first commit that carried what that stage produces, and never at or beyond the stage the room has reached -- under the rule written into the file as "backfilled". The status line is a side effect of the mailbox, and a delivery's status should describe the room delivered, not the next job | 11 Sep; version 2 on 13 Sep; backfill 382b8e9; audit 8bc38d3, 3bd6741 | The stages no check can derive. Eleven accepted rooms showed every earlier stage blank. On 17 September the user asked whether it was up to date; 27 rooms were wrong, and the wrong statuses had hidden 38 guides nobody had built |
| `scene/design.js` with `/design/rooms.json` | Folds live package metadata under the scene files. A large object that a package paints into its room (`largeObjects`: the ladder lying across the rift) keeps its place there, and `graybox.js` makes it only a pick target over a painting. Since 17 September a package says which of its large objects the picture actually has, and when: by default in every view, `paintedWhen: "<condition>"` only in that state, or `painted: false` never, in which case the entry only gives the place a thing that long lies. | 11 Sep (8d141b0); `largeObjects` 3561b07; `paintedWhen` 36b0f66 | The build ignored the designs. The ladder, moved into the Admin Corridor rooms by the rules, was a loose grey box on the first drop anchor. `largeObjects` was then doing two jobs -- "the picture already draws this" and "something big lies here" -- and the page read the second as the first: the ladder is listed in eight rooms and painted in one, so everywhere else it was a pick target a player could take but never see, Storage West, where it starts, included |
| Compare mode (`dashboard/preview.js`) | A comp laid over the built view, with the lens as a slider | 12 Sep (180a41f) | To measure a comp's lens |
| Acceptance gate (`POST /design/accept`) | The user's verdict and note; "changes" hands the room to Norm | 12 Sep (8f18eac); hand-back c85ec12 | "a user acceptance gate on whether it looks right" |
| Review page (`uat.html`, `dashboard/uat.js`) | One room as the game draws it, with its own states as switches, and notes on what has no art. A true/false setting gets two switches, each its own condition, and the page lists any fabric drawn in front of a whole painting. A `candidate` parameter hangs a picture that is not yet registered, in place of a registered view or as a new view or variant; nothing is written. A new candidate with no camera given takes the one written beside the picture (its `.json` sidecar); a new side counts as built from the registered level views, and a new cap from every level view. Since 16 September the switches come out in the order the package lists its states, which is the order the game reaches them, with two switches that read the same collapsed into one, and the page evaluates a room's state grades, so lighting that changes with a state shows. Since 17 September a room with a sky has a time-of-day row (daylight, dusk, night) at the top of the switches, driving the same clock as the Light panel. Since 22 September the list of fabric drawn in front of a painting leaves out masks, which write depth only and draw no colour | 12 Sep (45adc47); true/false switches 7d48522; over-painting e5bebb7; candidates ef4782e, 09470dc; the sidecar camera 2376655; switch order and duplicates 93985e0, 06ea054; state grades 3fb0e81; time of day 980949a; masks left out d18fe697 | The user wanted to toggle states while reviewing. Lawanda's two Alfie switches ticked together and could not bring the car in. Storage West's four new views all hung at one view's bearing, and the room read as not whole. The Escape Pod showed fifteen switches in geometry order with two pairs that meant the same thing, and Underwater's four-stage climb drew at one brightness on the page while the real game graded it correctly. Once painted rooms drew their fabric back as a depth-only mask (21 Sep), the masks were counted as drawn in front, and Waiting Area's real door slab sat under about 38 false warnings |
| The review page's "glimpse" tag (`dashboard/uat.js`) | Marks a switch whose painted change is tiny and far off, with its size and its arc in the tooltip. Measured, not declared: each variant is drawn against the view it departs from at 200 px and the changed area counted, so under one per cent of the frame is a glimpse. It applies to every room already delivered. Rewritten on 19 September to answer the question the user actually asked: the tag now names the ROOM the moving part is in, read from the scene ("in another room, Deck Nine", using the game's own room names), or the distance where the scene names no room, and it sits beside the "not painted" tag rather than instead of it, because both can be true of one switch | 17 Sep (a5c7239); the room named, 19 Sep (not yet committed) | The user, after the Admin Corridor South ladder: "whenever i complain about a object missing because the toggle isn't working, it could be for this reason ... label the toggle like (glimpse)". That ladder is painted and that switch works; it is 66 x 49 px of a 1672 x 941 frame. The page's other two tags both say the artwork CANNOT answer a switch; this one says it can, and the answer is tiny. The measured version missed the case that prompted the complaint: a door or a lit panel in the next room, seen through a wide opening, covers well over a per cent of the frame and so was never tagged. Worse, for three asks over two days the tag never appeared at all -- one line suppressed it for any switch with no painted variant, which was every switch the user was looking at -- and each fix was verified in node instead of on the page: "THIS IS THE 3RD TIME IVE HAD TO TELL YOU AND I HAVE NEVER ACTUALLY SEEN IT WORK" (19 Sep) |
| The review page's Light panel | Every lighting value in use and where it came from, with live sliders and pickers, reset, and "copy as JSON" in the metadata's shape | 14 Sep (d1f27aa) | Time of day did nothing on the paintings |
| `styles.html` (retired) | Deck Nine built three ways, then a fourth, plus a lens calibrator | 12 Sep (66025be); removed 45adc47 | To choose a visual direction by looking |
| `scripts/check_dashboard.mjs` | A browser check of the dashboard, the review page, the mailbox panel and the gate round trip | 11 Sep (5c9a0eb); 1594334 | Also taught never to touch real verdicts |
| "Play from here" (`?room=ID` in `game.js`, linked from the review page) | Starts the game after the opening and jumps to the room being reviewed; an id the world does not have is said and ignored | 22 Sep (a8c5fdb7) | The user: "i need a way when reviewing a room for me to enter play mode directly in that room so i can navigate around and look for continuity" |
| `scripts/check_status.mjs` | Works out who holds each room from the mailbox, and fails the build when the dashboard says Greg holds work he does not | 22 Sep (8e0b2b32) | The Rec Area and the Library were both shown as Greg's when nothing of theirs was with him |
| Mailbox panel (`dashboard/mail.js`) | Open asks by addressee, unread counts, latest messages | 14 Sep (c4022cc) | To see who waits on whom |
| `scripts/stage-stamp.mjs` | Fills a room's missing stage dates from the artefact each stage leaves behind -- the package file, a blockout, registered views, paint-progress, the review receipt. Nothing is invented and no existing date is touched; `mail.mjs` runs it on every status move, just before the stage milestones | 21 Sep (0f108704) | The stage cards counted a stage done only where something had written a date for it, and nothing ever wrote `views` when a room's last view was registered or `reviewed` when review-room hung it whole. The drift was large: 68 rooms fully painted with no `views` date, 41 with a review receipt and no `reviewed` date, and the counts went to 103 and 86 of 105 the moment they were backfilled |

## Building rooms

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `scene/graybox.js`, `scene/view.js` | Builds each room and renders it from the fixed eye; now places actors, items, plates and variants. Since 14 September an open container shows its contents where the room's metadata says (`contentsAt`), as their own pictures. A variant whose picture is null is a state the picture does not show, and draws the thing's box. Since 16 September `labelOffset`, in metres, moves a thing's name plate up or down, for objects and for globals alike, and may be given with no position, so a shared global can drop its plate in one room without claiming where the thing stands. Since 17 September a room painted as a SINGLE level view cannot be turned past where its painting reaches: the clamp is derived -- a room whose package registers exactly one level view and has its paintings hung -- and the allowance is computed per frame from the camera, sideways and up and down alike, with the picture's shape measured off the hung plate's own texture. Also since 17 September name plates are laid out each frame and any that would overlap are dropped, the one under the cursor kept; a lamp stamped with a neighbour's id survives a room's authored lighting; and a see-through container marked `showsContents` does not draw its contents, which stay an invisible click target. Since 22 September an actor may opt in to its authored height with `anchorHeight` (carried through by `design.js`); without it an actor stands on the floor exactly as before, and only the Escalator uses it | 9 Sep; many changes since; `anchorHeight` 4eceecff; `contentsAt` b04ccf6; the null variant 48e8d62; `labelOffset` 09d786a; the single-view clamp e9044a9, 3004c34; label layout a04aeb5; lamp stamps deb9b3c; `showsContents` 481f647 | The renderer everything hangs in. The canteen in the Kitchen's dispenser was "a grey square next to the dispenser unit". The ladder's picture is the collapsed ladder, and extended it is not that. "Set of controls" sat over the pod's porthole and "crevice" over the key on the floor: the pod's plate went down 0.70 m and the crevice's up 0.6, because the rule is away from what matters, not downward. The seat inside the pod's safety web is painted as one frame, and the yaw carries over from the room the player came from, which has a ring of four: face the webbing as you climb in and the camera kept pointing where nothing is painted. The first fix derived the sideways limit and typed a constant for the vertical one -- 0.35 radians where the arithmetic gives 0.066 -- which walked five times past the edge of the plate onto bare fabric, and that is the blocky ceiling and floor the user reported next. The Escalator's Floyd, authored on a tread 6.67 m up a 17.6 m shaft, was drawn at the foot of it with his label and no robot, because the height of an actor's position was dropped |
| `scene/parts.js`, `scene/materials.js` | Rooms as JSON parts: shell, bay, drum, frame, stair, pipe, panel, light, prop, then panorama, plate, cutout, decal, trail, open, sea, rock, spiral, prism and solid stair, and since 14 September `vault`, a barrel vault seen from both sides. Pipe elbows can be flush, and there is a charcoal safety-web surface. Since 16 September a window is drawn in `viewport-glass`, near black on purpose, so that it reads as a HOLE in every blockout. Since 17 September a drum may take a `ceilingHole`, so a stair can climb through its ceiling | 12 Sep (8f18eac); outdoors aec9b63, 59faffa, b1d830f; `vault` 929d9c2; `viewport-glass` 51595e7; `ceilingHole` f383a3d | About 50,000 tokens a room by hand was too dear. The Escape Pod's ribbed cabin, seen from Deck Nine, could not be built from the kit. A pane drawn in the same grey as the wall around it was painted as one more pale panel, and the porthole went where one could plausibly go |
| `scene/textures.js` | Canvas-drawn surfaces and signage | 12 Sep (66025be) | The stylised option |
| `scene/lighting.js` | The day-phase rig, authored interior lighting, and one sun per sky room. Since 16 September a room may author its own fog -- colour, near and far -- which wins over the sky's and doubles as the background in an enclosed room, and `sea` takes `fog: true` | 12 Sep (d10a414, be88f13); sun 2957d55; `lighting.fog` 9c23f3f | The scene ignored the authored light. Underwater has no sky to inherit a fog tone from, and the sky rule's distances are an order of magnitude too far for water |
| The plate (in `parts.js`) | Hangs a painting on the plane it was projected onto, feathered at the edges | 12 Sep (e41b1a6, 6c4b874) | Painted rooms you can still look around |
| Panorama part and "try a panorama" control | Wraps an equirectangular image round the eye | 12 Sep (21e4776) | The DR-088 trial, withdrawn |
| `dashboard/turn.js` | Lens fitting, exposure levelling, seam solving, `clearance()`, `planRing()`, `fitLenses()`, state variants, and `partsForTurn()`, which the review page and the game share to pick a painted room's views for the current state. `hungViews()` says which reference images a room hangs, for the game and the site build alike. A fabric part counts as painted when its condition is made of clauses of a painted state | 12 Sep (45adc47); c036e0e; `clearance()` cba06f2; 69cc078, 87ed19e; `partsForTurn()` shared f31720b; the painted-clause rule 7d48522; `hungViews()` fd8c0b5 | Seams the user could see. Later, Lawanda's Betty car drawn as a black slab in front of the whole painting |
| `scene/occlusion.js` | Which of the parts drawn over a whole painted turn the eye can actually see: a segment from the eye to a grid of points on the part, against the shell and bay walls (with their openings), floors, ceilings and solid props the paintings show. A part a wall hides entirely is dropped; one only a door's leaves hide is drawn only while they are open (its condition gains `and not (<leaves>)`); one with any point in view is drawn as before. `partsForTurn()` applies it to props and lamps. Tests `scripts/tests/occlusion.mjs` | 18 Sep | The Bio Lab's ceiling lamps, carried in the Bio Locks' scenes for what their door and window look into, came out as white slivers across the locks' painted walls with the lab lights on: over a whole turn nothing writes depth for the walls the paintings show, and writing it would hide the paintings themselves |
| `scripts/tests/turn.mjs`, `parts.mjs`, `design.mjs`, `lighting.mjs` | Tests for hanging, parts, placement and lighting. `design.mjs` perturbs two real packages on purpose; since 17 September it writes each original beside the file first and restores any left over from a killed run before it does anything else | 12 Sep onward; restore on kill c9d698f | Each fix held by a test. A `finally` block covers a throw but not a kill, and the release kills a test that passes five minutes |
| `scripts/tests/precheck.mjs`, `scripts/tests/lawanda.mjs` | The first pins the pre-delivery check's six false alarms against a package and pictures it builds itself, and that it still flags a painting which invents geometry. The second compares the Lawanda Platform's two cuts: the copy the Escalator sees may state less than the original, never anything different | 17 Sep (af52f62) | `precheck` was the tool leaned on hardest and trusted least, with no test at all. Nothing compared the two copies of the platform |
| `data/lens-fits.json` | Each view's lens as measured from its overlaps with its neighbours | 12 Sep (c036e0e) | The generator never hits the lens it is asked for |
| `scripts/audit-turns.mjs` | Audits every package's view set against its openings from geometry alone | 12 Sep (4c01c37, 69cc078) | "plan cameras around openings before generating images" |
| `scripts/review-room.mjs` | Evidence for a painted room: lenses, gains, seams, overlaps, 16 shots, a contact sheet and a switch sheet. It warns when fabric is drawn in front of a whole painting, in the opening state and with each switch flipped on its own, and says outright when a straight line is not the room's own edge. Since 14 September Greg runs it before every delivery of a joined room. `--candidate ID=PATH` reviews a view before it is registered: the page hangs it and the join scan follows it. A brand-new candidate, such as a room's first cap, counts as built from every registered view, so its joins are read, and it takes its camera from its sidecar. Since 15 September it warns when a cap is left behind by a state change: a level view's variant changes something inside the view's top or bottom fade band, and the cap, which shows through there, has no variant for that state (`capStates`). Since 16 September, where the lens fitter trusts no view, it says so and marks the overlap residuals unreliable, so a room of fog, water and rounded rock is judged on its contact sheet. Its warning that fabric is drawn in front of a whole painting is a static scan of the part list, which asks whether a part's condition holds and never whether the part can be seen: it must be confirmed by rendering the room and looking before it is acted on. Since 18 September it takes state presets, so a compound state such as the shuttle's MOVING, which no single switch can set, is reachable on the page and shot in the review sheets. Since 19 September it runs the toggle gate on the rows it has already shot, and a switch already on in the opening state is switched OFF rather than skipped, so that it is exercised at all; restoring the page after each row tries three times and no more, and says which switch was left untested if it fails. Since 20 September it grows the browser window until the stage is back at full size before the switch rows are shot, and re-centres the drag point when it does, because the page lays the switch list under the stage and a long list squeezes the canvas rather than moving it; `turnTo` turns in bounded steps rather than one drag past the canvas edge; and every run writes a receipt, `scripts/out/review/<ROOM>/reviewed.json`, listing every registered view by content hash, which is what the mailbox checks before a room may go to the user. Since 21 September it also reports any switch no painted variant branches on: with no variant for the state `turn.js` re-hangs the base texture, so the picture is identical and nothing warns. Things that draw their own picture, such as a character cutout, are exempt, and a state the package declares `noVisibleChange` is not a missing painting | 12 Sep (836ebfe); state presets 79853eb; sheet fbf74fc; switches 4b11b8c; the plainer line warning 8f671d5; over-painting e5bebb7; switches reset between rows 1a61d7a; `--candidate` ef4782e; a new view's joins 09470dc; the sidecar camera 2376655; caps against states 48e8d62; unreliable overlaps 581a9c1 | Evidence from a script, not from reading a screen. A band that stopped where its guide ran out was sent on as an inherited edge. Once registering became Norm's, Greg could not see the Escape Pod's floor-cap line with the room whole. The first `--candidate` read none of a new cap's joins, and passed four lines on the Mess Hall ceiling. Storage West's floor cap showed the open door leaf through the shut door's fade, and the review passed it; on its first run the new check found the same in accepted Deck Nine. One Underwater pair measured 1.029 with four views hung and 1.111 with six, with neither picture repainted. The over-painting warning was reported to the user as a real bug on 16 September and was not one. The grown window came from four shuttle cabs reporting fifteen and sixteen failing switches each: the switch shots were 108 to 143 px tall against the level shots' 559, so the gate was judging a sliver. The receipt came from Underwater D2, which reached the user as "whole, six of six" with its sixth view inherited from an earlier session, and had rocks floating in it: "I should NEVER be seeing something without your review". The dead-switch check came from five separate "the toggle does nothing" reports on 21 September that were one cause between them, a state nobody had painted; its own first report named a room that was already correct, because it did not yet read `noVisibleChange` |
| `scripts/graybox-view.mjs` | Renders the room's fabric from the eye at an exact camera, caps included. `--switch` renders it in a state, by ticking the review page's own switch, for a painted variant's fabric. Since 17 September it writes a SIDECAR beside every blockout -- room, bearing, pitch, lens and size -- so a seed painted from it can be registered straight from that file. Since 18 September `--fill` raises the room's fill light for that one picture only and records it in the sidecar, so that a room the package lights very dimly (the Bio Lab at 0.07, the Radiation Lab at 0.09) still gives a blockout that can be painted from | 13 Sep (1adf50a); caps 5a2f7ac; `--switch` 7d48522; the sidecar bf7993c; `--fill` 12fcb1a | Seeds painted over the real room, not fitted afterwards. Eight checked and good seeds could not be registered because nobody had recorded what camera they were painted at, and recording it when the picture is made closes the whole class of problem |
| `scripts/blockouts/` | Hand-measured props for the dorm and SanFac shells | 12–13 Sep (6f29ab0); re-measured 41b26a8 | Guides for rooms without fabric |
| `scripts/fabric-kalamontee.mjs` | The Kalamontee living quarters as one layout, with each room cut from it. Its second pass adds the equipped Kitchen, the re-furnished Mess Hall, and only the doors each eye can see. It reaches west to the Plain Hall, the Conference Room, Booth 1 and Dorms A and B, and east to the Corridor Junction and the Elevator Lobby, with scenes for the Rec Corridor, the Rec Area, the Corridor Junction and Storage West; `--also` adds rooms for check renders. Its third pass makes full rooms of the Conference Room, Booth 1, Admin Corridor South and SanFac E, and lays out the admin wing north of the Junction: the Admin Corridor rent apart by an 8 m rift open to the sky, Admin Corridor North across it, the Systems Monitors, the Small and Large Offices, the Plan Room and the Transportation Supply, with the ladder across the rift as a switch. Its fix pass closed pinholes and slivers of sky, and lit the Transportation Supply by the player's lamp. Since 17 September it lays the Systems Monitors' three switched status strips as overlays (`wwwroot/assets/overlays/MONITOR-STRIP-LIVE.png`, cut from Greg's own green strip), each on its own flag, seen from that room and through the Admin Corridor's doorway; `scripts/tests/turn.mjs` pins both rooms. Also since 17 September it cuts the descent to the shuttle: the Lower Elevator, the Waiting Area, the Kalamontee Platform, the Escalator and the Fork. Every area builder now stamps the lamps it cuts for a neighbour with that neighbour's id, so a room's authored lighting replaces only its own. Since 18 September it cuts the Elevator Lobby, with its two cars gated on the room's states, and Booth 2 | 14 Sep (f0f07e4); second pass 0ccf924; Storage West b0507c1; third pass d204d24; fix pass f555c75; strip overlays 43ab305; the descent 7e21721; lamp stamps deb9b3c; Elevator Lobby and Booth 2 c99b747 | Doorway glimpses must be the neighbour's real geometry; a switch for a door out of sight did nothing. The admin wing was Norm's first metadata batch |
| `scripts/fabric-lawanda.mjs` | The same for Lawanda Platform, the three Systems Corridors and the Infirmary, with their neighbours as far as each eye can see. Since 17 September it also cuts the five rooms off the Systems Corridors (the Repair Room, Planetary Defense, Planetary Course Control, the Library Lobby and Physical Plant Two) and the project hallway (Project Corridor, Project Corridor West, SanFac F, and Project Corridor East's mouths), with placeholder shells for rooms whose packages are still under review. On the night of 17 to 18 September it gained Booth 3, in a notch capped at 3.36 m cut from Physical Plant Two; the south cluster (Project Corridor East's moved mouth, the Computer Room, the Projcon Office as a real room, and the Mini Booth); and then the whole lab cluster: the Main Lab built in full, Lab Storage, both Radiation Lock halves and the Radiation Lab, both Bio Lock halves and the Bio Lab, the Lab Office, the Auxiliary Booth and both Cryo rooms. It throws if the mouths stop lining up or the chamfer gap between the Computer Room and the Main Lab stops being two wall thicknesses. Every change was checked by re-rendering the accepted views of each changed scene and comparing them pixel by pixel | 14 Sep (0905776); the five 00e09f3; the hallway ae32e50; Booth 3 419986b; the south cluster 2b7fa48, 0ae66ea; the lab cluster 12fcb1a | The next cluster |
| `scripts/fabric-tower.mjs`, `scripts/fabric-shuttle.mjs` | The tower top (Tower Core, Upper Elevator, Comm Room, Observation Deck, Helipad, Helicopter) and the Alfie shuttle with its two cabins, each as one layout. The Tower Core, cut by hand before, moved into the tower's frame with its seven parts unchanged. Since 18 September the shuttle frame also holds Betty and her two cabins, at the north berth both stations already had, with Alfie's fit-out mirrored north to south; Alfie's scenes came out byte-identical | 17 Sep (7e21721); Betty fd6e6b7 | Fourteen rooms that open onto each other; a hand-written scene could not show a neighbour's own geometry |
| `scripts/fabric-computer.mjs` | Station 384 and the three rooms of the strip inside the computer, in a frame of their own; the void is each package's black fog | 17 Sep (ae32e50) | At the packages' first light the strip barely separated from the black, so the fill is 1.6 and the fog 8-16 m, which still hides the relay and the station from the middle room |
| `scripts/fabric-underwater.mjs` | The climb out of the water as art rooms above the seabed: Underwater D2, D1 and D0, with eyes at 6.4, 11.2 and 15.6 m under a surface at 16.2 m, each with its scene (`viewOf`) and six blockouts. The rooms are installed inert, and the game draws one only once it is accepted | 18 Sep (093214c, the DR-115 blockouts); scenes installed ddc51a1 | The user decided the water is deeper than the package's datum, so each step of the climb rises about 5 m and needs its own paintings |
| `scripts/fabric-westwing.mjs` | Cuts the West Wing's fabric from the Courtyard's own scene, seen from the West Wing's eye, with the barrier lowered to 1.0 m | 14 Sep (89e0dab) | The accepted Courtyard painting already shows the West Wing |
| `scripts/fabric-feinstein.mjs` | The Gangway, Deck Eight, the Reactor Lobby and the Brig as one layout, placed against Deck Nine's accepted fabric, which it reads but does not regenerate. Since 18:41 on 14 September it also builds the Escape Pod, fitted to Deck Nine's accepted glimpse through the pod door. The pod was re-cut three times as the user settled its layout: the webbing made nets round an open nest with a second eye inside it (15 Sep), then one web mass with a single eye and the window opposite (16 Sep), then the user's own plan under DR-112 -- webbing left, console on the back bulkhead, a 2.4 x 1.1 m porthole right, the webbing half the pod, and the seat inside the web a room again. The writer SHIFTS every part by the room's `eye` as it writes the scene, so a package's `eye` is in scene coordinates while its sizes, openings and exits are in room coordinates; the pod's metadata says so, because the two frames read as a contradiction | 14 Sep (34139d7); the pod 335881e; the nets 1c39c8a, 9a3ca23; one eye 2c10fd5; DR-112 ac9bfdc; the two frames written down 5a680b3 | The four rooms the user chose to paint must meet Deck Nine's accepted views. The player spends the whole launch and descent inside the webbing, so that is where the eye has to be. "Correcting" the metadata to match the writer moved the camera 0.30 m and every blockout by 47 to 81 per cent of the frame, and a seed pair was painted from them before anyone looked at a render |
| `scene/grade.js`, `scene/skymatte.js` | A time-of-day grade applied inside each painting's own material (and cutouts' and decals'), and a matte for what is sky | 14 Sep (d1f27aa) | "none of the time of day ddls work" |

## Painting tools

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `scripts/stage-canvas.mjs` (retired) | Put a seed on a bigger canvas for the generator to fill | 12 Sep (70fe4c4); removed 6f29ab0 | Failed: the generator drew a new room |
| `scripts/underpaint.mjs` | The view-building tools. `view` makes a guide from accepted views and blockout; `restore` puts accepted pixels back and scores them; `fit` reads a seed's camera; `through` keeps an edit inside a doorway; `mirror` flips a room; `joins` cuts joins out to look at; `caps` makes cap guides; `seam` takes out a ridge; `state` makes sea-only variants. On a cap, `seam` lays the accepted pixels fully up to the boundary and runs its whole ramp outward into the new part (keep 0, into 40), since the ring is drawn over the cap's accepted side; `--crest` takes out a line along the join that is not a ridge, such as a dark groove | 12 Sep (6f29ab0); fit 65aafd3; through and mirror be02fbf; joins 6d05242; caps a221b24; state, seam and guards 7654323; `state` refuses a change above the horizon and names `through --opening` 1a61d7a; seam on a cap and `--crest` cb94c04 | "Holes fail... Complete pictures work". Norm once sent Greg to `state` for a shuttle berth; Greg caught it. The Kitchen's ceiling-cap line grew with every mend made the level-view way. `caps` fills only what the level views reach: on 17 September, on a complete four-view ring, it projected 69 per cent real accepted pixels and left the centre of each cap flat blockout colour, which is why a cap still has to be painted |
| `scripts/lib/views.mjs`, `png.mjs` | Camera pixel arithmetic, and PNG input and output with no dependencies. Since 16 September `restore` reports the blend band's own texture beside its score (`BAND_TEXTURE`) and says when the number is noise, because the score is a correlation and can only measure edges that exist | 12 Sep (6f29ab0); `BAND_TEXTURE` 6e897eb | Testable in Node, no browser. The band score ranked an invented wall pattern above the correct plain wall it replaced, 0.244 against 0.195 |
| `scripts/lib/seam.mjs` | Profiles a join and names a STEP or a RIDGE | 14 Sep (7654323) | The Crag floor seam survived three repaints |
| `scripts/lib/state.mjs` | Classifies a sea-only composite | 14 Sep (7654323) | The generator repainted rock when raising the sea |
| `scripts/check-states.mjs` | Flags state variants that changed fixed structure | 14 Sep (604c0b9); 6e50f9b | Structural drift passed the numbers |
| `scripts/check-state.mjs` | One state variant against its registered master: the near field's STRUCTURE, not its colour, with the relight reported separately. Calibrated on the master itself -- a pure relight scores 0.994 to 1.000, a one-pixel shift 0.701 -- so 0.95 separates a relight from geometry that moved or texture that was generated again. The glass region is found automatically, the pane being the one near-black thing in a blockout, so Greg runs it before delivering | 16 Sep (87292e6) | The pod's first three descent states looked right one at a time and scored 0.842 to 0.860: the near field had been repainted rather than reused. The player sits on that one frame for fourteen turns and would have seen the room crawl every time the counter ticked |
| `scripts/precheck.mjs` | Every check a picture has to pass, in one command, before anyone registers it or sends it anywhere, deciding from the file itself rather than from a flag: is there a sidecar beside it carrying bearing, pitch and lens; `check-seed` against the blockout; `check-state` against the accepted view; and SIBLINGS, a variant against the other variants of the same view. `node scripts/precheck.mjs <ROOM> --all` takes a whole reference folder. A picture at a registered bearing is read as a revision (`_R2`, a seed, a completed guide) and checked against the blockout, or as a state variant and checked against its base, by its name; files in a delivery that are not pictures are named and skipped rather than failed on. It takes `PF_PACKAGES` so a test can hand it a package of its own. Since 19 September it also measures the MATERIAL of a repaint against the held pixels beside it (`lib/relief.mjs`), reads the `@` form of a state name, and matches a picture to its own room's blockout before matching on the request number. Since 20 September it reads a cap's camera: a request number is not a bearing, a cap has no bearing at all but a pitch, and its geometry lives in the square `_SQ_BLOCKOUT` sidecar. Since 22 September it reads a picture's bearing only from the view part of its name, before the `@`, with a test that fails on the old code | 17 Sep (bd53f0d); six false alarms fixed and a test, af52f62; revisions told from variants, and non-pictures skipped, a24701b; the view before the `@`, d18fe697 | The checks existed; a single thing to run did not, and that cost real rounds -- a seed sat unregistered for want of a sidecar, pod states were delivered before anyone measured whether they held the near field, and three views were hung at a typed lens. The sibling check is new: `check-state` only ever compares a variant with its base, which is how a provisions panel came to change shape as its contents were taken out. The room-first match was found when two Underwater depth seeds were measured against a third room's blockout and flagged for geometry they had painted correctly (19 Sep). Twelve caps then sat unregisterable for a day because `DR-106_STORAGE-EAST-CAP-UP_GREG.png` read as bearing 106 and was judged against the room's registered TURN-106; with the parser fixed, fifteen caps across eleven rooms were registered within the hour, each at its own lens -- 131, 132 and 133 in three rooms of that batch, since a cap sits at whatever its ring's reach sets (20 Sep). Elevator Lobby's `000@UPPER-ELEVATOR-UP` was then taken for the room's UP cap, because its state's name ends in `-UP` (22 Sep) |
| `scripts/check-seed.mjs`, `scripts/lib/check-seed.mjs` | A painted seed against its fabric render, structure only (walls, corners, shelves, furniture, doorways; not rust or rivets). It prints PASS or FLAG, with each region the painting invents or drops, and a sheet to look at. It cannot see a doorway's beyond, which is too small a patch of the frame | 14 Sep (bfc3234) | Storage West 200 painted a five-level shelf unit the fabric does not have, caught only by eye. One of the four process changes the user asked for |
| `scripts/locate.mjs`, `scripts/lib/locate.mjs` | Where a room's thing lands in a painted view, with the game's own camera; a face of it rectified to a plate at a stated scale; and plate and view pixels converted both ways. Without `--force` it writes over no existing file and nothing under `RoomPolishInstructions/` | 14 Sep (577723a) | The Kitchen dispenser's damage guide was hand-read and about 30 plate pixels out. A test run while it was built wrote over Greg's delivered overlay, which was restored by its hash |
| `scripts/lib/shots.mjs` | Straight-line brightness steps along joins in game shots, and state drift. `capStates` finds a cap left behind by a state change, offline from the metadata and the pictures: 8 px blocks compared by mean brightness, sky left out by the game's own matte, and only changed blocks that join into a piece, so regenerated grain, glints and waves pass. Since 19 September it also holds THE TOGGLE GATE (`toggleDiff`, `toggleExemption`, `toggleGate`, `roomAccepted`, driven from `review-room.mjs`, tests `scripts/tests/toggle-gate.mjs`): every switch in a room has to move something on the screen before the room may go for acceptance. It diffs the switch rows the review has already shot, bearing by bearing, and takes no new renders. Scale is not part of the test -- a 36 by 10 px prop moving 34 m up a corridor passes -- the question is zero or not zero. A switch may be exempted in the room's metadata, but only in writing and with a reason, since a bare flag would be a silent opt-out of the one check that looks at pixels. An accepted room is not gated again | 14 Sep (604c0b9); `capStates` 48e8d62; the toggle gate, 19 Sep (not yet committed) | Replaced the costly four-reviewer passes. Storage West's ghost door leaf was seen only by eye. The gate is the user's rule, written after three fixes in one morning were verified in node, reported as working, and were not working on their screen: "run through the toggles and observe that SOMETHING, ANYTHING changes on the screen", and if nothing changes that is a pre-acceptance fail. Their two constraints came with it: do not get caught in an infinite loop, and "an already accepted room is accepted, no need to rerun through this gate" |
| `level-join.mjs` (scratch, in the untracked `scripts/out/`, not in the repository) | Levels a hard brightness step at a join in place: each row's gain from the kept side, ramped over the pixels to its left | 14 Sep | A step of about 20 levels on the equipped Kitchen's west wall |
| `scripts/level-join.mjs`, `scripts/lib/level.mjs` | The scratch leveller made real: it levels a step where a view's held pixels begin, or at a plate boundary, following the boundary wherever it runs (a column, a diagonal seed frame, a cap's rim); the held side is not touched, and a join with no step is left byte for byte | 18 Sep (05a700f) | The scratch one levelled one column only, and so left the Kitchen's diagonal floor join stepped. On 18 September the user asked that Greg's work be image work only, and Norm try a compute fix before any repaint ask (mail 20260918-105329-dev-0462) |
| `scripts/warp-walls.mjs`, `scripts/lib/warp.mjs` | Moves a painting's walls to where the fabric has them, by a piecewise-linear warp, when the texture is right and only the geometry is wrong; then the view is restored as usual | 18 Sep (05a700f); tests `scripts/tests/warp-walls.mjs` | The generator paints a car symmetric about the frame when the eye is off-centre. Norm chose to warp the Lower Elevator's 180 R2 rather than ask Greg for a third round (mail 20260918-140305) |
| `scripts/lamp-off.mjs`, `scripts/lib/lamp-off.mjs` | A painted lamp put out as an overlay: a patch cut square-on from one registered view, its lit face darkened and its cast taken out, hung as a cutout with a `when` so no view is repainted | 18 Sep (05a700f); tests `scripts/tests/lamp-off.mjs` | The user found the Computer Room's "computer fixed" switch made no difference; the red light now goes dark when the computer is fixed, there and in the Mini Booth |
| `scripts/lib/relief.mjs`, `scripts/tests/relief.mjs` | Is a repaint inside a guide's mask the same MATERIAL as the held pixels beside it? The luminance is band-limited to features roughly 5 to 19 px across -- coarser than pixel noise, finer than the shading of a wall -- and compared locally and in pairs: the frame is cut into 64 px tiles, and a tile counts only where it holds enough new pixels near the mask boundary and enough held pixels just outside it, so the two readings are of the same wall a few pixels apart. The headline is the median of the per-tile ratios. It reports and never fails | 19 Sep (not yet committed); read by `precheck.mjs` | Reactor Access Stairs came back twice with a swirled stucco where the room is smooth poured concrete, and both times the numbers said it was fixed: the only texture measure was a 3 by 3 high-pass, which cannot see a loop 8 to 15 px wide at all. In that band the fault had got worse between the rounds, not better. On its first live round the new measure named the next fault -- a flat fill with no surface at all -- before the pictures were opened |
| `scripts/deliveries.mjs` | Norm's side of a delivery from Greg: from a mail to pictures registered in the room's metadata. Each mail has its own manifest, and a picture whose name carries a state is treated as a variant: prechecked, never restored or registered by the script. Since 19 September it knows compute's provenance tags (`NORM`, `COMPOSITE`, `CAPFILL`, `LEVELLED`, `MERGE`) from state names, reads the `@` spelling of a variant, and retries a registration that hits a transient Windows file error while another fanned-out agent holds the metadata open | 18 Sep (3d325d5); tests `scripts/tests/deliveries.mjs`; caps 469d1f8 | It replaced three scratch scripts that had run the pipeline off the record for a night, and designs out their two accidents: a failed merge re-registered a rejected delivery, and a state variant was parsed as a repaint. On 19 September a whole batch had to be run by hand because its files used the `@` spelling the parser did not read, and two registrations were nearly left half-written under fan-out |
| `scripts/view-diff.mjs` | Proves a layout change moved nothing accepted: renders every registered view and cap of the given rooms before and after, and pixel-diffs the two sets | 18 Sep (3d325d5) | Every fabric change since DR-106 had been proved this way by a script that lived in a session scratchpad |
| `scripts/cap-fill.mjs`, `scripts/lib/cap-fill.mjs` | Fills a cap's new centre by compute, for a floor or ceiling of regular plating: the border's broad light carried across, its own tread quilted in, its seams run straight on, and its rivets cloned at each junction. The result is then restored and scored like any painting. Since the evening of 18 September it has `--sat` (a copper tread needs about 0.7), `--sat-floor`, `--src-rect` and `--no-lines`, and its colour test ignores near-black. A cap with fixtures, lamps, ribs that change direction or objects in the hole stays with Greg. Since 19 September it first asks whether the border holds any of the cap's own floor or ceiling to copy (`capSurface`), and refuses with the arithmetic written out when it does not; a refusal for too few clean source blocks names its own remedy, a larger block size | 18 Sep (79853eb); first used on the Helipad 5377ab3; flags ddc51a1; tests `scripts/tests/cap-fill.mjs` | The generator paints its own grid instead of carrying a plating on, and restore rejects it. On 18 September it filled about ten caps that would otherwise have been Greg's, whose image work is the costly part. The surface check came from Storage East: 4 by 4 m, the eye at 1.6 m, a 132-degree cap lens, so the floor reaches 349 px from the centre against a hole that already runs to 363 -- no deck in the border at all, and the fill laid the walls' plating down as decking. A precondition, not a quality problem. On the night of 20 September eleven more floor caps were filled, restored and registered in one run, out of fifteen rooms that had closed rings and no caps -- roughly thirty images if all of it had gone to Greg. The limit is the picture, not the number: the Library's UP cap filled at a grain ratio of 0.837 and would have passed a numeric test, while cutting the ceiling fixtures into L and T shapes, so it went to Greg |
| `scripts/room-through.mjs`, `scripts/lib/room-through.mjs` | A door-state variant by compute, with no painting. In `through` mode each doorway ray from the room's eye runs on into the neighbour's box, placed where the room's scene places it, and the point it meets is read from the neighbour's registered views from the neighbour's own eye, so the parallax is right. What has no depth (a hall beyond an arch) is read as a picture on that opening's plane. In `leaves` mode the shut door is the slab the neighbour paints on its side, reprojected and given the room's wall colour. `--ss n` supersamples a far, minified neighbour (the Bio Lab's view of the Bio Lock aliased into moire without it). `--floor-detail` gives a near floor the neighbour's views only saw grazing its tread from the neighbour's DOWN cap; `--haze` with `--haze-grade` puts a neighbour's air (the Bio Lab's fungicide mist) in the doorway; `--ungrade` pre-divides a doorway by the room's own state grade, since a grade is one set of numbers for every picture a room hangs and cannot spare an already-lit neighbour; `--door-bay` takes a far door across an open joint. `--door DOOR` is scene mode, for rooms built wall by wall (the Main Lab's octagon, Bio Lock West's raked end): the doorway is where the eye meets the door's shut leaves first, the neighbour is placed by the same leaves in its own scene, and each ray runs on through the neighbour's own fabric. A `-holes` picture marks prop faces no painting sees. The result is then blended with `underpaint.mjs through --opening` | 18 Sep (the scratch `cryo-through.mjs`, made a tool the same night); tests `scripts/tests/room-through.mjs` | The Cryo Elevator's LOWER-OPEN and DOOR-SHUT variants of TURN-000 and TURN-DOWN (DR-106), where the door opens 60 m down onto the Cryo Anteroom. The anteroom was already painted, so the view through the door could be computed rather than asked of Greg. Rerun as a tool, it reproduces the four registered composites byte for byte |
| `watch-design.sh` and the lens fitter `fit.mjs` (scratch, not in the repository) | A ledger watcher, and the jamb-edge lens fit behind bd6b9e4 | 12 Sep | One-off tools that later became real ones |

## Actors and items

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `scripts/cutout.mjs`, `scripts/lib/cutout.mjs` | Cuts a figure out of a flat backdrop into real transparency, with un-mixed rims | 13 Sep (3178db6); rims db8539d | The generator cannot write transparency |
| `scripts/sprite.mjs` | The step between a delivered sprite that already has real transparency and the runtime: trim to the thing, resize, and downsample on premultiplied colour. With a test | 16 Sep (06ea054) | `cutout.mjs` is for a figure keyed off a flat backdrop and does not scale. Averaging straight RGBA drags whatever sits under the transparency into the edge, usually black, which reads as a dark fringe once the thing hangs in a lit room. The pod's survival kit and towel drew as yellow boxes until they had pictures |
| `scripts/tests/cutout.mjs` | A synthetic figure with a known answer | 13 Sep (3178db6) | Holds the cutout rules |
| `data/characters.json` | Registry of cutouts: image, height, footprint, variants with conditions; since 14 September things as well as actors (the padlock, the canteen, and since 15 September the can and the ladder, cut from Greg's item set). An entry with no picture of its own draws only in its variants' states, so a painted thing can show an overlay in one state (the dispenser's damage). A picture with `facing` lies flat on the front of the thing's box, blended, and only over a painting. Since 17 September `showsContents` marks a see-through container whose picture already paints what is inside it (the Infirmary's medicine bottle), and the red spool and the bottle have pictures. Since 18 September every portable object with a picture file has an entry, 43 in all, and `flat` lays a thing drawn from above (a card, a paper, a folded uniform) on its surface as a decal instead of standing it up as a cutout | 12 Sep (8d0d88f); things 85624f8; overlay-only entries b04ccf6; the canteen e3f9cd4; `facing` 85fad5d; the can and the ladder 48e8d62; the spool, the bottle and `showsContents` 481f647; `flat` fd6e6b7; all 43 0f79f1c | Characters as flat figures facing the eye. Turned to the eye, the dispenser's damage would have sat 24 degrees off the dispenser's face |
| `cutout`, `decal` and `trail` parts | A figure facing the eye; flat patches and mirrored strips on the floor | 12 Sep (8d0d88f, 4c01c37, 72c427f) | Blather, the ambassador, the slime |
| `RoomPolishInstructions/items/build-manifest.mjs` (Greg's, untracked) | Audits item-art coverage against the game's portable objects | 12 Sep | DR-099's item library |
| `scripts/drop-sweep.mjs` | Every carriable thing at every drop anchor, by arithmetic over the packages, the world and the art, with no browser: a thing with no picture, a picture whose height disagrees with the height its room placed it for, how tall a thing actually comes out in pixels at each anchor at the review page's own camera, and an anchor outside its room's box. A raised anchor is explicitly not a fault -- the Escalator's are on its steps, the Gangway's on its deck | 21 Sep (a2044c0e) | The review page shoots a room in its opening state, so it cannot see a dropped thing at all: the user, not the gate, found the Tool Room's floating magnet, Storage East's speck of an oil can and Lab Storage's brown block. An object is room-independent, so sweeping 44 things beats reviewing 104 rooms. It measured the user's "3x bigger" as a rule rather than three notes: 25 of 44 things under 20 px at the median anchor, every keycard at 5 px, the Admin Corridor key at 3 |

## Working together

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `HANDOFF.md` | Session state and the prioritised backlog | 9 Sep (bd88341) | "so an agent can pick it up later" |
| Norm's memory files (not in the repository) | Rules that persist across sessions | 10–14 Sep | Lessons that must not be relearned |
| `scripts/mail.mjs`, `scripts/lib/mail.mjs`, `RoomPolishInstructions/mail/` | The mailbox: typed JSON messages, hashed files, numbered asks, read cursors, and ledger and dashboard updates. A room that is with the user stays there: Greg's status cannot move it, only the user's verdict or Norm. When a room arrives on the user's plate, the mailbox sends the user one Telegram line (`notify`), and since 21 September a line goes out when work lands in Greg's queue too -- a new ask addressed to him, or a room handed to him -- carrying the total number of open asks, so the size of the queue is in the line and not just the change. Since 19 September it also runs the stage milestones, and since 21 September `stage-stamp.mjs` just before them, a status move no longer being the only thing that changes those counts. Since 20 September it REFUSES four things, which is contract Version 7: an ask with no `--settled-by` (dom, pixels, render or test) saying what would show it done, a close with no matching `--evidence`, a changes verdict with no `--fault` saying what is wrong rather than where it is (and no `--regions`, the boxes drawn on the blockout, where the fault names a rectangle), and a third change ask on one view. The count begins with the contract, so work that was mid-flight when it went live is not parked. It also refuses `--status-with user` unless `review-room.mjs` has left a receipt covering every registered view at the pixels it has now | 13 Sep (d06b858); the user's rooms kept b9dd06d; the Telegram line 871c1c3; Version 7 d3a83eb; the count not retroactive 286d721; the review receipt 0e07730 | The prose ledger was slow and costly. The Brig dropped off the user's queue when a note from Greg followed Norm's hand-over. The refusals come from the workflow review of 20 September: 195 extra rounds on 818 views, about a quarter of the image budget spent painting something twice, three fixes reported done on evidence from below the fault, three rounds lost to region boxes too small to hold the fault, and a circuit-breaker that had never once fired in 223 requests. A refused send writes nothing, which is the mailbox's oldest guarantee, so a refusal names the request rather than setting its status itself |
| `scripts/notify.mjs`, `scripts/lib/notify.mjs` | One-line Telegram updates to the user through the user's Telegram bot: sent by the mailbox when a room reaches the user, and by hand with `--blocked` when Norm is blocked on the user. Every line starts with the bot's @mention. The token and the chat are kept in the user's profile, outside the repository, and are never printed. A failure never stops the caller, and no test or test mailbox sends a line | 14 Sep (871c1c3); the mention f8be897 | The user asked for "significant one-line updates", to start with "a new room is pending my review and you are blocked by something" |
| `scripts/stage-milestones.mjs`, `scripts/lib/milestones.mjs` | Tells the user on Telegram when the dashboard's Seeds, Views or Reviews card crosses a multiple of ten. Run by `mail.mjs` after every status move. The counts are the dashboard's own, cumulative repair included, so the message cannot disagree with the card the user is looking at. A first run on a project already under way announces nothing: it writes down where the counts are and waits. A count that falls -- a room withdrawn from the user -- lowers the remembered decade silently, so crossing it again is still a milestone | 19 Sep (not yet committed); tests `scripts/tests/milestones.mjs` | The user, 19 September: "on the dashboard categories seeds, views, reviews i want you to send telegram ... a message anytime those number cross a multiple of 10, e.g. Seeds now past 90 / 105" |
| `scripts/review-server.mjs`, `scripts/lib/review-server.mjs` | One shared scratch copy of the site on port 5099 for every review tool; the last tool out stops it. Since 22 September an old build folder is renamed before it is removed, and a folder that a running server still holds, which Windows refuses to rename, is left whole | 14 Sep (604c0b9); busy folders left whole, d18fe697 | Review servers had locked the user's app. On 22 September a recursive remove had deleted everything in a build folder except the dll a running server held, so the folder looked unbuilt and every fanned-out agent's next build into it failed on the locked file (MSB3027); Norm found five review servers left running from 20 and 21 September |
| Background watchers (Claude Code, not in the repository) | Wake Norm when Greg writes: first the ledger, then the mail folder, then the user's verdicts | 12 Sep onward | "a sort of automated dialogue" |
| Workflow scripts (Claude Code, not in the repository) | Multi-agent reviews and parallel build tracks | 13 Sep onward | Adversarial review before relying on work |
| Subagent fan-outs (Claude Code) | Area porters, playtesters, reviewers, audit batches | 9 Sep onward | Speed on independent work |

## The history

| Tool | What it is for | Born | Why |
|---|---|---|---|
| `wwwroot/wiki.html`, `wwwroot/wiki/wiki.js` | Shows these pages with a small Markdown renderer that escapes everything. The side contents list each page once | 14 Sep (da4378d); side contents 1aebaa9 | To publish the history with the game. The side contents had listed every page twice |
| `wwwroot/wiki/playtest-scores.svg`, `playtest-scores.json` | A chart and its data: every playtester's best score by round, and round twelve's two runs from the start, life by life. Each number is read from a session transcript and checked against the tester's diary, and the JSON names the lines. Built by a background agent; shown in [Playing it](playing-it.md). There is no script that draws it: on the evening of 18 September the historian added rounds 13 to 16 to the JSON from their transcripts and inserted their four rows in the chart's own layout, moving the lower panel down | 14 Sep (1aebaa9); rounds 13 to 16 on 18 Sep, not yet committed | The user wanted "a graphical representation of how far each playtester got in the game", and on 18 September asked for the most recent playtest graphs to be updated |
| `scripts/mail-flow.mjs`, `scripts/lib/asks.mjs`, `wwwroot/wiki/mail-flow.svg`, `mail-flow-points.svg`, `mail-speed.svg`, `mail-flow.csv` | Three charts read straight out of the mailbox: the cumulative flow of every ask ever raised, the same flow weighted by a retrospective estimate of each ask's size, and how long an ask waits to be answered. `lib/asks.mjs` holds the ask table and the sizing rule, and says at the top what it cannot see. Every time comes from the stamp the message records for itself, in Eastern time: the ids are UTC and reading them as local put the whole of the first pass four hours out. `node scripts/mail-flow.mjs --csv` rewrites all three and the data; shown in [How we work](how-we-work.md#the-flow-of-asks) | 17 Sep (a2c413c); sizing and the speed chart 0829752; the clock fixed 2f6d7bc; redrawn to 06:03 on 18 September, to 19:40 on 21 September and to 12:40 on 22 September, at the user's request | The user asked for a cumulative flow diagram of the two agents' work, then whether effort could be estimated after the fact |
| `scripts/work-categories.mjs`, `wwwroot/wiki/work-share.svg`, `work-rework.svg`, `work-painting.svg`, `work-friction.svg`, `work-friction-rate.svg`, and the CSVs `work-categories.csv`, `work-share.csv`, `work-friction.csv` | What each day's work went on, in eight categories: metadata and canon, layout and blockouts, painting, objects and sprites, tools and engine, playtesting, playtest review and fixes, and rework. It reads three sources that count different things: mailbox asks, sorted by keywords; changed lines in git, by file class; and playtest commands, from the transcripts. Each source is first made a share of its own total, and then each day is scaled to 100%. Rework is decided by what happened to an ask: a view delivered again, an R2 or later, work sent back or withdrawn, or a change from the user. A second chart shows rework's share of each day's asks, and a third splits painting into seeds, ring views, caps and variants. Two more sort every playtest friction row by kind, round by round, and count negative rows per 100 commands. `node scripts/work-categories.mjs --csv` redraws all five; shown in [How we work](how-we-work.md#what-the-work-went-on) and [Playing it](playing-it.md#what-the-testers-logged-round-by-round). A round with no entry in `playtest-scores.json` is taken to be still in play and drawn dashed, with an asterisk. Its git window is "since 2026-09-10" with no time of day, which git starts at the current time of day on the 10th, so the first day's bar and the git line totals move with the hour the charts are drawn (noticed 22 Sep; not yet changed) | 18 Sep (469d1f8); the two friction charts and `work-friction.csv` redrawn that evening with rounds 13 to 16, the three work charts redrawn through 21 September with the page's figures re-checked against them, and again at midday on 22 September | The user asked for graphs of the categories of work, and agreed the design with Norm. The same morning they asked how the makeup of the playtest diaries changed over time |
| `scripts/tests/wiki.mjs` | Checks the renderer, and that every link between pages lands on a real page | 14 Sep (da4378d) | The likeliest mistake when appending |
| The wiki's pages (`wwwroot/wiki/*.md`) | Ten pages: the overview, six narrative pages, this catalogue, the timeline and the journal. Six researcher agents wrote notes from git, the ledger, the mailbox and the transcripts, a writer composed the pages, and two fact-checkers found 70 problems, all fixed | 14 Sep (13597c0) | "going forward lets keep a running history of our activities which we will publish alongside the game" |
| The historian agent (`.claude/agents/historian.md`, at the repository root) | After each session or milestone, writes a dated [journal](journal.md) entry from git, the mailbox, the ledger and the dashboard | 14 Sep (da4378d) | "a running history of our activities" |
